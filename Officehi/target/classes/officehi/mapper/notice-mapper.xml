<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.groupware.officehi.repository.NoticeRepository">
	<insert id="insertNotice" useGeneratedKeys="true" keyProperty="noticeNo" parameterType="NoticeDTO">
		INSERT INTO NOTICE(title, date, content)
		VALUES (#{title}, curdate(), #{content})
	</insert>
	
	<select id="findAll" resultType="com.groupware.officehi.dto.NoticeDTO">
		select notice_no, date, content, title
		from notice
		order by date desc;
	</select>

	<select id="findById" parameterType="Long" resultType="com.groupware.officehi.dto.NoticeDTO">
		select *
		from notice
		where notice_no = #{noticeNo}
	</select>

	<select id="findByContent">
		SELECT *
		FROM NOTICE
		WHERE CONTENT LIKE CONCAT('%', #{content}, '%')
	</select>
</mapper>